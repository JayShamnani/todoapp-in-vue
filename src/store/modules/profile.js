const state = {
  profileState: [],
};
const getters = {
  Profilestore: (state) => state.profileState,
};
const actions = {};
const mutations = {};
export default {
  state,
  getters,
  actions,
  mutations,
};
